# Workflow n8n — cambios del 2026-07-25

Nodos: **143 → 151**  ·  workflow `1npQWgfgBBIwVuxX` (NovTurnAI)

> Las keys `service_role` salen ocultas en este reporte. En los dos JSON van completas (son las tuyas, ya estaban hardcodeadas antes).


## Nodos nuevos (10)

### + Uso - Registrar Basic
- tipo: `n8n-nodes-base.httpRequest`
- url: `https://kwpaaqdkklwwfslhkqpb.supabase.co/rest/v1/rpc/record_usage`
- onError: `continueRegularOutput`
- body:
```json
={
  "p_business_id": "{{ $('Negocio - Obtener').first().json.id }}",
  "p_tokens_in": {{ Math.ceil(((($('Historial - Formatear').first().json.history || '') + ($('Buffer - Campos').first().json.message || '')).length + 1200) / 4) }},
  "p_tokens_out": {{ Math.ceil((($('Agente - Basic').first().json.output || '').length) / 4) }},
  "p_messages": 1
}
```

### + Uso - Registrar Pro
- tipo: `n8n-nodes-base.httpRequest`
- url: `https://kwpaaqdkklwwfslhkqpb.supabase.co/rest/v1/rpc/record_usage`
- onError: `continueRegularOutput`
- body:
```json
={
  "p_business_id": "{{ $('Negocio - Obtener').first().json.id }}",
  "p_tokens_in": {{ Math.ceil(((($('Historial - Formatear').first().json.history || '') + ($('Buffer - Campos').first().json.message || '')).length + 1200) / 4) }},
  "p_tokens_out": {{ Math.ceil((($('Agente - Pro').first().json.output || '').length) / 4) }},
  "p_messages": 1
}
```

### + Uso - Registrar Enterprise
- tipo: `n8n-nodes-base.httpRequest`
- url: `https://kwpaaqdkklwwfslhkqpb.supabase.co/rest/v1/rpc/record_usage`
- onError: `continueRegularOutput`
- body:
```json
={
  "p_business_id": "{{ $('Negocio - Obtener').first().json.id }}",
  "p_tokens_in": {{ Math.ceil(((($('Historial - Formatear').first().json.history || '') + ($('Buffer - Campos').first().json.message || '')).length + 1200) / 4) }},
  "p_tokens_out": {{ Math.ceil((($('Agente - Enterprise').first().json.output || '').length) / 4) }},
  "p_messages": 1
}
```

### + Tool - Perfil Paciente Enterprise
- tipo: `n8n-nodes-base.httpRequestTool`
- url: `=https://kwpaaqdkklwwfslhkqpb.supabase.co/rest/v1/rpc/get_patient_profile?p_business_id={{ $('Negocio - Obtener').first().json.id }}&p_patient_id={{ $if($('Paciente - Obtener Datos').isExecuted, $('Paciente - Obtener Datos').first().json.id, $('Paciente - Crear').first().json.id) }}`
- descripción para la IA: "Perfil del cliente recurrente: visitas, no_shows, servicio_frecuente, ultima_visita y prioridad. Llamar SOLO si el cliente ya es conocido, para personalizar el trato."

### + Pipeline - Actividad Basic
- tipo: `n8n-nodes-base.httpRequest`
- url: `https://kwpaaqdkklwwfslhkqpb.supabase.co/rest/v1/rpc/pipeline_touch`
- onError: `continueRegularOutput`
- body:
```json
={
  "p_business_id": "{{ $('Negocio - Obtener').first().json.id }}",
  "p_patient_id": "{{ $if($('Paciente - Obtener Datos').isExecuted, $('Paciente - Obtener Datos').first().json.id, $('Paciente - Crear').first().json.id) }}",
  "p_flag": "activity",
  "p_summary": {{ JSON.stringify((($('Agente - Basic').first().json.output) || '').replace(/\s+/g, ' ').trim().substring(0, 120)) }}
}
```

### + Tool - Métodos de Pago Basic
- tipo: `n8n-nodes-base.httpRequestTool`
- url: `=https://kwpaaqdkklwwfslhkqpb.supabase.co/rest/v1/payment_methods?business_id=eq.{{ $('Negocio - Obtener').first().json.id }}&active=eq.true&select=label,is_cash&order=sort_order`
- descripción para la IA: "Formas de pago que acepta el negocio (efectivo, tarjeta, transferencia, etc.). Llamar SOLO si el cliente pregunta cómo puede pagar."

### + Pipeline - Actividad Pro
- tipo: `n8n-nodes-base.httpRequest`
- url: `https://kwpaaqdkklwwfslhkqpb.supabase.co/rest/v1/rpc/pipeline_touch`
- onError: `continueRegularOutput`
- body:
```json
={
  "p_business_id": "{{ $('Negocio - Obtener').first().json.id }}",
  "p_patient_id": "{{ $if($('Paciente - Obtener Datos').isExecuted, $('Paciente - Obtener Datos').first().json.id, $('Paciente - Crear').first().json.id) }}",
  "p_flag": "activity",
  "p_summary": {{ JSON.stringify((($('Agente - Pro').first().json.output) || '').replace(/\s+/g, ' ').trim().substring(0, 120)) }}
}
```

### + Tool - Métodos de Pago Pro
- tipo: `n8n-nodes-base.httpRequestTool`
- url: `=https://kwpaaqdkklwwfslhkqpb.supabase.co/rest/v1/payment_methods?business_id=eq.{{ $('Negocio - Obtener').first().json.id }}&active=eq.true&select=label,is_cash&order=sort_order`
- descripción para la IA: "Formas de pago que acepta el negocio (efectivo, tarjeta, transferencia, etc.). Llamar SOLO si el cliente pregunta cómo puede pagar."

### + Pipeline - Actividad Enterprise
- tipo: `n8n-nodes-base.httpRequest`
- url: `https://kwpaaqdkklwwfslhkqpb.supabase.co/rest/v1/rpc/pipeline_touch`
- onError: `continueRegularOutput`
- body:
```json
={
  "p_business_id": "{{ $('Negocio - Obtener').first().json.id }}",
  "p_patient_id": "{{ $if($('Paciente - Obtener Datos').isExecuted, $('Paciente - Obtener Datos').first().json.id, $('Paciente - Crear').first().json.id) }}",
  "p_flag": "activity",
  "p_summary": {{ JSON.stringify((($('Agente - Enterprise').first().json.output) || '').replace(/\s+/g, ' ').trim().substring(0, 120)) }}
}
```

### + Tool - Métodos de Pago Enterprise
- tipo: `n8n-nodes-base.httpRequestTool`
- url: `=https://kwpaaqdkklwwfslhkqpb.supabase.co/rest/v1/payment_methods?business_id=eq.{{ $('Negocio - Obtener').first().json.id }}&active=eq.true&select=label,is_cash&order=sort_order`
- descripción para la IA: "Formas de pago que acepta el negocio (efectivo, tarjeta, transferencia, etc.). Llamar SOLO si el cliente pregunta cómo puede pagar."


## Nodos eliminados (2)

### − Turnos - Confirmed
- tipo: `n8n-nodes-base.supabase`
- motivo: la consulta de turnos se consolidó en **una sola query** (`date_start` del mes + `status <> cancelled`),
  igual que cuenta el trigger `enforce_appointment_limit`. Con una sola rama, el `Merge` sobra.

### − Merge
- tipo: `n8n-nodes-base.merge`
- motivo: la consulta de turnos se consolidó en **una sola query** (`date_start` del mes + `status <> cancelled`),
  igual que cuenta el trigger `enforce_appointment_limit`. Con una sola rama, el `Merge` sobra.




## Nodos modificados (34)

### ~ Historial - Obtener
- `limit`: 10 → 100

### ~ WA - Mensaje Inválido
- onError: `STOP` → `continueRegularOutput`

### ~ WA - Saludo Simple
- onError: `STOP` → `continueRegularOutput`

### ~ WA - Queja
- onError: `STOP` → `continueRegularOutput`

### ~ WA - Pide Humano
- onError: `STOP` → `continueRegularOutput`

### ~ WA - Negocio Inactivo
- onError: `STOP` → `continueRegularOutput`

### ~ Flow - ¿Plan Activo?
- condición:
  - **antes:** `={{ ['active','trial'].includes($('Negocio - Obtener').first().json.plan_status) && $('Negocio - Obtener').first().json.active === true && ($('Negocio - Obtener').first().json.plan_expires_at === null || DateTime.fromISO($('Negocio - Obtener').first().json.plan_expires_at) > DateTime.now()) }}`
  - **ahora:** `={{ ['active','trial'].includes($('Negocio - Obtener').first().json.plan_status) && $('Negocio - Obtener').first().json.active === true && $('Negocio - Obtener').first().json.ai_paused !== true && ($('Negocio - Obtener').first().json.plan_expires_at === null || DateTime.fromISO($('Negocio - Obtener').first().json.plan_expires_at) > DateTime.now()) }}`

### ~ WA - Emergencia
- onError: `STOP` → `continueRegularOutput`

### ~ WA - Domicilio
- onError: `STOP` → `continueRegularOutput`

### ~ WA - API Limit
- onError: `STOP` → `continueRegularOutput`

### ~ WA - Error Tipo Mensaje
- onError: `STOP` → `continueRegularOutput`

### ~ WA - Classifier Fallido
- onError: `STOP` → `continueRegularOutput`

### ~ Historial - Urgencia
- `fieldsUi`: {"fieldValues":[{"fieldId":"patient_id","fieldValue":"={{ $if($('Paciente - Obtener Datos').isExecuted, $('Paciente - Obtener Datos').first().json.id,… → {"fieldValues":[{"fieldId":"patient_id","fieldValue":"={{ $if($('Paciente - Obtener Datos').isExecuted, $('Paciente - Obtener Datos').first().json.id,…

### ~ Historial - Queja
- `fieldsUi`: {"fieldValues":[{"fieldId":"patient_id","fieldValue":"={{ $if($('Paciente - Obtener Datos').isExecuted, $('Paciente - Obtener Datos').first().json.id,… → {"fieldValues":[{"fieldId":"patient_id","fieldValue":"={{ $if($('Paciente - Obtener Datos').isExecuted, $('Paciente - Obtener Datos').first().json.id,…

### ~ Turnos - Scheduled
- `filters`: {"conditions":[{"keyName":"business_id","condition":"eq","keyValue":"={{ $('Negocio - Obtener').first().json.id }}"},{"keyName":"created_at","conditio… → {"conditions":[{"keyName":"business_id","condition":"eq","keyValue":"={{ $('Negocio - Obtener').first().json.id }}"},{"keyName":"date_start","conditio…

### ~ Flow - ¿Superó límite?
- `conditions`: cambios menores de metadatos

### ~ WA - Error Limite Alcanzado
- onError: `STOP` → `continueRegularOutput`

### ~ Set - Total Conversaciones
- `assignments`: {"assignments":[{"id":"6de46756-384d-492e-a10f-7880020dd5c7","name":"total_conversaciones","value":"={{ $input.all().length }}","type":"string"}]} → {"assignments":[{"id":"6de46756-384d-492e-a10f-7880020dd5c7","name":"total_conversaciones","value":"={{ Number($json.messages ?? 0) }}","type":"number…

### ~ Flow - ¿Conversaciones Ok?
- condición:
  - **antes:** `={{ $json.total_conversaciones }}`
  - **ahora:** `={{ Number($json.total_conversaciones) }}`

### ~ WA - Error Limite Alcanzado 2
- onError: `STOP` → `continueRegularOutput`

### ~ Agente - Basic
- prompt: se añadieron 443 caracteres de reglas nuevas

### ~ Agente - Pro
- prompt: se añadieron 443 caracteres de reglas nuevas

### ~ Agente - Enterprise
- prompt: se añadieron 604 caracteres de reglas nuevas

### ~ WA - Respuesta Basic
- onError: `STOP` → `continueRegularOutput`

### ~ WA - Respuesta Pro
- onError: `STOP` → `continueRegularOutput`

### ~ WA - Respuesta Enterprise
- onError: `STOP` → `continueRegularOutput`

### ~ WA - Error Basic
- onError: `STOP` → `continueRegularOutput`

### ~ WA - Error Pro
- onError: `STOP` → `continueRegularOutput`

### ~ WA - Error Enterprise
- onError: `STOP` → `continueRegularOutput`

### ~ Modelo - Enterprise
- `modelName`: (sin valor) → "models/gemini-2.5-flash"

### ~ Modelo - Pro
- `modelName`: (sin valor) → "models/gemini-2.5-flash"

### ~ Conversaciones - Contar
- onError: `STOP` → `continueRegularOutput`
- `operation`: "getAll" → (sin valor)
- `tableId`: "history" → (sin valor)
- `matchType`: "allFilters" → (sin valor)
- `filters`: {"conditions":[{"keyName":"business_id","condition":"eq","keyValue":"={{ $('Negocio - Obtener').first().json.id }}"},{"keyName":"created_at","conditio… → (sin valor)
- `url`: (sin valor) → "=https://kwpaaqdkklwwfslhkqpb.supabase.co/rest/v1/usage_counters?select=messages&business_id=eq.{{ $('Negocio - Obtener').first().json.id }}&period=e…
- `sendHeaders`: (sin valor) → true
- `specifyHeaders`: (sin valor) → "json"
- `jsonHeaders`: (sin valor) → "{\n  \"apikey\": \"<SERVICE_ROLE_KEY>\",\n  \"Authorization\": \"Bearer <SERVICE_ROLE_KEY>\",\n  \"Content-Type\": \"application/json\"\n}"
- `options`: (sin valor) → {}

### ~ Modelo - Embedding Pro
- `options`: {"maxOutputTokens":10} → {"maxOutputTokens":150}
- `modelName`: (sin valor) → "models/gemini-2.5-flash-lite"

### ~ Modelo - Embedding Enterprise
- `modelName`: (sin valor) → "models/gemini-2.5-flash-lite"


## Reglas añadidas a los prompts de los agentes

### Agente - Basic
```
- Si CreateAppt devuelve error con SCHEDULE_CLOSED → "Ese día no atendemos. ¿Qué otro día te queda bien?" y pedir otra fecha. Si devuelve SCHEDULE_DAILY_CAP → "Ya no queda cupo para ese día. ¿Probamos con otro?" y pedir otra fecha. NUNCA reintentar la misma fecha.
- Si el cliente pregunta cómo pagar, si aceptan tarjeta o transferencia → llamar la herramienta de métodos de pago y listar SOLO los que devuelva. NUNCA inventar formas de pago.
```

### Agente - Pro
```
- Si CreateAppt devuelve error con SCHEDULE_CLOSED → "Ese día no atendemos. ¿Qué otro día te queda bien?" y pedir otra fecha. Si devuelve SCHEDULE_DAILY_CAP → "Ya no queda cupo para ese día. ¿Probamos con otro?" y pedir otra fecha. NUNCA reintentar la misma fecha.
- Si el cliente pregunta cómo pagar, si aceptan tarjeta o transferencia → llamar la herramienta de métodos de pago y listar SOLO los que devuelva. NUNCA inventar formas de pago.
```

### Agente - Enterprise
```
- Si CreateAppt devuelve error con SCHEDULE_CLOSED → "Ese día no atendemos. ¿Qué otro día te queda bien?" y pedir otra fecha. Si devuelve SCHEDULE_DAILY_CAP → "Ya no queda cupo para ese día. ¿Probamos con otro?" y pedir otra fecha. NUNCA reintentar la misma fecha.
- GetPerfil: llamarlo SOLO si el cliente ya es conocido (aparece en HISTORIAL o tiene citas previas), máximo 1 vez por conversación, para personalizar el trato.
- Si el cliente pregunta cómo pagar, si aceptan tarjeta o transferencia → llamar la herramienta de métodos de pago y listar SOLO los que devuelva. NUNCA inventar formas de pago.
```
